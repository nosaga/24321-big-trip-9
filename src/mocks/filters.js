export const filters = [
  {
    filter: `Everything`,
    isChecked: Boolean(Math.round(Math.random()))
  },
  {
    filter: `Future`,
    isChecked: Boolean(Math.round(Math.random()))

  },
  {
    filter: `Past`,
    isChecked: Boolean(Math.round(Math.random()))

  }
];
